# Claudeへの引き継ぎ

## 最初に渡す文章（そのままコピー可）

```text
GitHubの mochipj2025/Codex-mochi を、ブランチ codex/copy-first-guide-assets から引き継いでください。
最初に CLAUDE.md と experiences/first-step/docs/SPEC.md を読み、git status で作業中の変更を確認してください。

方針は「学習よりすぐ使える、簡単、迷わない」です。
初心者にはキー操作や長い説明を先に出さず、用意した文章をコピーしてCodexに貼る入口を中心にしてください。
案内役はもちスラくまと芽ちゃんです。完成済みの透過素材とサイトアイコンを使ってください。

まず npm run guide:test と npm run guide:build を実行し、npm run guide:dev で見本を開いてください。
今回は実ブラウザでの動作確認もお願いします。1枚作る導線、5枚制作の途中再開、コピーできない場合、スマホ幅・文字拡大を確認し、迷う点を直してください。
画像を生成・保存できたことは、サイトから自動判定できません。実際の操作結果と案内を混同しないでください。

続いて既存サイトの app/page.tsx・共通ヘッダー・メタデータを確認し、この入口とアイコンを取り込む変更を別コミットで用意してください。
既存の教材・勉強会・録画・保護されたスキル図書館への導線と保護を保ってください。
Substack管理画面URLを公開録画リンクとして掲載しないでください。
公開サイトへのデプロイやmainへのマージは、差分と確認結果を提示した後、私の指示を受けて行ってください。

実装済み・検証済み・公開済み・未確認を区別して短い日本語で報告してください。
このPCで作業する場合、D:/00000/brain/README.md と直接関係するメモを読み、同日・同じ話題の進捗へ追記してください。
brainへアクセスできない場合は作業場所に一時保存し、brain未反映と明記。認証情報は記録しないでください。
```

## 開発メモ

Node.jsはリポジトリの既存条件に合わせ22.13以降。静的見本の起動・ビルド・テストには追加npmパッケージ不要。アイコン再作成時だけPythonとPillowが必要。

ルートのdev / build / testは既存教材サイト用。入口はguide:dev / guide:build / guide:testを使う。dist、ローカル認証ファイル、Sites短期トークンはコミットしない。

本体の既存テストは、移植で本体を変更するときに実行する。静的見本だけの確認が本体の動作保証になるわけではない。

今回の範囲は、既存リポジトリの専用ブランチへの入口・透過3点・アイコン・仕様・再現コマンドの収録。公開トップは未移植。Claudeへ自動メッセージは送っていない。
